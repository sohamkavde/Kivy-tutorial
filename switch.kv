<MySwitchapp>:
    orientation:"vertical"
    Label:
        id:my_label
        text:"This is button and it is on!"
        font_size:42
        color:(0,1,1,1)

    Switch:
        active:True
        on_active:root.activefunction(self.active)